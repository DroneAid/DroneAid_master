<phone:PhoneApplicationPage
    x:Class="DroneAidWP81.Layouts.PFacebookLogin"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d"
    shell:SystemTray.IsVisible="False">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="#3b5998">
        <ProgressBar IsIndeterminate="True" IsEnabled="True" Foreground="White" Background="#19FFFFFF"/>
        <TextBlock VerticalAlignment="Center" Margin="0 50 0 0" TextAlignment="Center" Style="{StaticResource PhoneTextTitle2Style}" Text="Loading..." Foreground="White" />
    </Grid>

</phone:PhoneApplicationPage>